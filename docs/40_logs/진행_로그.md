# SmartFarmProject 진행 로그

## 2026-06-22

### 오늘 한 일
- `references/스마트팜.txt` 내용을 검토했다.
- 기획 명세 PDF의 목표, 요구사항, 산출물 항목을 확인했다.
- 프로젝트를 End-to-End 스마트팜 자동화 시스템으로 정리했다.
- 프로젝트 폴더와 문서화 구조를 시작했다.

### 핵심 결과
- 필수 산출물 축을 `주제 / 기술조사 / 아이디어선정 / 기술스택 / SW구조 / WBS / R&R`로 정리했다.
- 초기 기능 블록을 `비전AI / Dobot / 컨베이어 / TurtleBot / 웹관제` 5개로 정리했다.
- Dobot은 석션컵으로 작물을 수확한 뒤 1번 카메라 앞으로 이동하고, 작물을 **120도 간격 3방향**으로 촬영한다는 점을 확인했다.
- 이후 불량 작물은 별도 상자에 담고, 정상 작물만 컨베이어에 올려 2번 카메라가 벨트 위 흐름과 위치를 확인한다는 흐름을 정리했다.
- 분류 기준은 `작물 3종 분류 + 썩음 여부 판별`이며, 학습된 비전 모델을 사용한다.
- 논의 결과, 현재 확정 방향은 `1번 카메라=작물 종류 분류 + 불량 판별`, `2번 카메라=라인 흐름 확인용 보조 비전`이다.

### 막힌 점 / 판단 필요
- 컨베이어 이후 최종 적재/폐기/배송 구조를 더 구체화해야 한다.
- 물뿌리개/비료 분사 기능의 기구 설계는 추후 별도 설명이 필요하다.
- ROS 2와 웹 서버 간 연동 방식을 정해야 한다.

### 다음 액션
- 아키텍처 보강 계획을 기준으로 `시스템 아키텍처 정의서 v2`를 작성한다.
- 전체 아키텍처 지도 / 런타임 흐름 / Modbus register ownership 다이어그램을 3장으로 분리해 만든다.
- README와 Obsidian에 새 아키텍처 문서 링크를 반영한다.
- 검토 후 Git commit/push한다.

## 2026-06-23

### 오늘 한 일
- `README.md`, `docs/진행_로그.md`, `docs/R&R_초안.md`, WBS 검토/재검토 메모, `references/WBS.xlsx`를 다시 확인했다.
- 이 쓰레드의 지원 범위를 **Dobot 파트 중심**으로 정했다.
- Dobot 전용 작업 문서 `docs/Dobot_작업_메모.md`를 추가했다.
- `시스템_데이터_흐름_초안.md`의 Dobot 제어 항목에 3방향 촬영 포즈, 정상/불량 분기, 홈 복귀를 반영했다.

### Dobot 기준 핵심 결과
- 현실 구현 기준 Dobot 시퀀스는 `수확 → 1번 카메라 3방향 촬영 → 비전 판정 대기 → 불량품 상자 또는 컨베이어 시작점 배치 → 홈 복귀`로 정리했다.
- Dobot 상태값 초안은 `idle`, `moving_to_crop`, `suction_on`, `moving_to_camera`, `pose_capture_0/120/-120`, `waiting_vision_result`, `moving_to_defect_box`, `moving_to_conveyor`, `release`, `returning_home`, `error`로 잡았다.

### 다음 액션
- Dobot 실제 모델/SDK/제어 방식을 확인한다.
- 수확 위치, 촬영 위치, 불량품 상자, 컨베이어 시작점 좌표 후보를 작성한다.
- 3방향 촬영을 Dobot 자세 변경으로 할지, 별도 회전 지그로 할지 결정한다.

### Dobot 추가 확정
- 모델은 **Dobot Magician(두봇 매지션)**, 제어 방식은 **ROS 2**로 확정했다.
- 좌표는 `(x, y, z)` 순서로 기록한다.
- 수확 위치 9개를 고정 좌표로 정리했다: `(60,180,-60)`, `(100,180,-60)`, `(140,180,-60)`, `(60,220,-60)`, `(100,220,-60)`, `(140,220,-60)`, `(60,260,-60)`, `(100,260,-60)`, `(140,260,-60)`.
- 촬영 위치는 `(240,0,0)`, 불량품 상자 위치는 `(240,140,0)`, 컨베이어 시작점은 `(160,-200,-10)`로 정리했다.
- 3방향 촬영은 석션컵 360도 회전을 이용해 `0도 → 120도 → -120도 → 0도 복귀`로 수행한다.
- 흡착 성공 여부는 우선 시간 기반으로 처리한다.
- 위 내용을 `docs/Dobot_작업_메모.md`에 반영했다.

### Dobot 이동/보정 방식 추가 확정
- 석션 ON 후 흡착 대기 시간은 **2초**로 정했다.
- 촬영 신호는 Dobot이 각도 도달을 알리고, 비전 노드가 촬영 완료를 응답하는 방식으로 정했다.
- `magician_ros2`에서 homing한 위치를 `(0,0,0)` 기준 HOME/READY로 둔다.
- 다만 TCP/실제 파지점 차이 때문에 Dobot 배치가 바뀌면 실제 현재 위치를 읽어 좌표를 다시 보정하기로 했다.
- 이동은 바로 목표점으로 가지 않고, z축을 먼저 올린 뒤 x/y 이동 후 내려가는 **jump move 방식**으로 진행한다.
- 위 내용을 `docs/Dobot_작업_메모.md`, `docs/작업_결정_메모.md`, `docs/시스템_데이터_흐름_초안.md`에 반영했다.

### Dobot 임시 테스트 방식 추가 확정
- jump move 안전 높이는 **z=100mm**로 정했다.
- 비전 파트가 당장 없으므로 촬영 완료 신호 대기와 비전 판정 결과 대기는 주석 처리한다.
- 임시 테스트에서는 각 촬영 포즈에서 2초 대기 후 다음 각도/동작으로 넘어간다.
- 위 내용을 `docs/Dobot_작업_메모.md`, `docs/작업_결정_메모.md`, `docs/시스템_데이터_흐름_초안.md`에 반영했다.

### Dobot 위치 재보정 코드 작성
- `/dobot_TCP` 토픽을 구독해 사용자가 `s`를 입력한 시점의 TCP pose를 저장하는 위치 보정 노드를 작성했다.
- 코드 위치는 `workspaces/지웅/ros2_ws/src/dobot_control_pkg/dobot_control_pkg/calibrate_positions.py`이다.
- 실행 엔트리포인트는 `ros2 run dobot_control_pkg calibrate_positions`로 추가했다.
- 캡처 순서는 `수확 좌하단 -> 수확 우상단 -> 촬영 위치 -> 불량상자 위치 -> 컨베이어 위치`이다.
- 수확 위치는 좌하단/우상단 두 점을 기준으로 3x3 격자를 자동 생성하고, 결과는 JSON/Markdown 파일로 저장한다.
- 기본 결과 파일은 `workspaces/지웅/ros2_ws/src/dobot_control_pkg/config/dobot_positions_latest.json`이다.
- `python3 -m py_compile`과 `colcon build --packages-select dobot_control_pkg` 검증을 완료했다.

### Dobot 위치 재보정 결과 확인
- 사용자가 캡처를 완료한 결과 파일 `workspaces/지웅/ros2_ws/dobot_positions_latest.json`을 확인했다.
- JSON 구조, 캡처 순서, 3x3 수확 위치 9개, 주요 위치 3개가 정상임을 검증했다.
- 캡처 시점 age는 0.002~0.036초로 최신 `/dobot_TCP` pose가 저장된 것으로 확인했다.
- 결과 파일을 코드에서 참조하기 쉬운 `workspaces/지웅/ros2_ws/src/dobot_control_pkg/config/dobot_positions_latest.json`에도 복사했다.
- `calibrate_positions.py`의 기본 출력 경로도 `ros2_ws`에서 실행 시 source package의 `config/` 아래로 저장되도록 보정했다.
- 코드 검증으로 `python3 -m py_compile`과 `colcon build --packages-select dobot_control_pkg`를 다시 통과했다.

### Dobot 단일 수확 테스트 노드 작성
- 실측 좌표 JSON을 읽어 단일 수확 테스트를 수행하는 `harvest_test.py` 노드를 작성했다.
- 실행 엔트리포인트는 `ros2 run dobot_control_pkg harvest_test`이다.
- 수확 인덱스는 사용자가 입력하며, 인덱스 부여 기준은 **x 내림차순 -> y 내림차순**이다.
- 구현 흐름은 `선택 수확 위치 -> z=100mm jump move -> 석션 ON 2초 -> 촬영 위치 -> 0/120/-120도 각도별 2초 대기 -> 0도 복귀 -> 정상 임시 분류 -> 컨베이어 -> 석션 OFF -> 홈 복귀`이다.
- `setup.py`에는 `harvest_test` console script와 config 설치 항목을 추가했고, `package.xml`에는 `dobot_msgs`, `action_msgs`, `ament_index_python` 실행 의존성을 추가했다.
- 검증으로 `python3 -m py_compile`, `colcon build --packages-select dobot_control_pkg`, `ros2 run ... --help`, `ros2 run ... --dry-run --harvest-index 9 --yes`를 통과했다.

### Dobot 수확 위치 재보정 방식 변경
- 실제 테스트에서 `240도` 회전 goal이 reject되어 촬영 회전 순서를 `0도 -> 120도 -> -120도 -> 0도 복귀`로 변경했다.
- 수확 위치 z는 기본 **-50mm**로 고정하기로 했다.
- 좌하단/우상단 2점 분할 방식이 부정확해, 수확 위치 9개를 전부 직접 찍는 `calibrate_harvest_positions.py` 노드를 추가했다.
- 새 엔트리포인트는 `ros2 run dobot_control_pkg calibrate_harvest_positions`이다.
- 이 노드는 기존 `dobot_positions_latest.json`의 카메라/불량상자/컨베이어 위치는 유지하고, `positions.harvest_grid`만 새 9점 값으로 교체한다.
- 검증으로 `python3 -m py_compile`, `colcon build --packages-select dobot_control_pkg`, `ros2 run calibrate_harvest_positions --help`, `harvest_test --dry-run`을 통과했다.

### Dobot 9점 좌표 반영 및 첫 jump move 보정
- 9개 수확 위치 캡처가 완료되어 `dobot_positions_latest.json`의 `positions.harvest_grid`가 실제 9점 x/y와 고정 z=-50mm로 갱신되었다.
- 최신 입력 인덱스표는 `harvest_1..9`가 그대로 x 내림차순 -> y 내림차순 순서로 매칭된다.
- 이전 동작에서 z축 상승이 안 보인 문제는 첫 이동 전에 코드가 현재 TCP 위치를 모르는 구조일 가능성이 있어, `harvest_test.py`가 시작 시 `/dobot_TCP`를 읽어 `current_pose`를 초기화하도록 수정했다.
- 수정 후 dry-run과 `colcon build --packages-select dobot_control_pkg`를 통과했다.

### Dobot 홈 위치 및 safe z 기본값 변경
- JSON `positions.home`에 홈 위치 `(150, 0, 100)mm`를 추가했다.
- `harvest_test.py`의 `--safe-z-mm` 기본값을 `100`으로 변경했다.
- 한 사이클 종료 후 컨베이어 위치에서 석션 OFF한 뒤 홈 위치로 복귀하도록 변경했다.
- 검증으로 `python3 -m py_compile`, `colcon build --packages-select dobot_control_pkg`, `harvest_test --dry-run`을 통과했다.

### 추가로 한 일 — 별도 비전 파트 정리
- `README.md`, WBS/R&R, 진행 로그, 작업 결정 메모, 시스템 데이터 흐름 초안을 비전 파트 관점에서 다시 확인했다.
- 별도 비전 정리 범위에서는 **비전 파트 중심**으로 문서를 분리했다.
- 비전 전용 작업 문서 `docs/Vision_작업_메모.md`를 추가했다.
- WBS 기준 비전 관련 작업을 `데이터 기획`, `AI 모델링`, `비전 제어`, `HW 세팅(2)`, `장치 제어`, `시스템 연동`으로 묶어 정리했다.

### 비전 기준 핵심 결과
- 1번 카메라는 `작물 3종 분류 + 정상/불량 판정`을 담당하는 메인 판정 카메라로 유지한다.
- 2번 카메라는 컨베이어 위 `존재/위치/타이밍/흐름 이상`을 확인하는 보조 비전 카메라로 유지한다.
- 1번 카메라 결과는 Dobot의 `불량품 상자 이동` 또는 `컨베이어 시작점 적재`를 결정하는 권위 소스로 둔다.
- 우선 설계 대상은 데이터셋/라벨링 기준, 3방향 이미지 저장 구조, YOLO 학습/추론 방식, 비전 결과 메시지 포맷이다.

### 비전 파트 다음 액션
- 실제 작물 3종 이름을 확정한다.
- 불량 기준이 썩음만인지, 흠집/크기/색 이상까지 포함하는지 정한다.
- PC 선검증 후 라즈베리파이 포팅 순서로 갈지 결정한다.
- 2번 카메라에서 RGB-D depth를 MVP에 사용할지, RGB 기반 감지만 먼저 구현할지 결정한다.

### 라즈베리파이 카메라 소켓 전송 테스트
- 작업 경로 `workspaces/지웅/vision`에 1차 소켓 통신 코드를 작성했다.
- 라즈베리파이 코드 `raspi_capture_send.py`: 카메라 연결 확인, H.264 영상 촬영, PC로 TCP 전송.
- PC 코드 `pc_receiver_streamer.py`: TCP 영상 수신, 파일 저장, OpenCV `imshow` 창 재생. 옵션으로 HTTP latest 영상/metadata 송출도 가능.
- 로컬 PC 내부 더미 영상 전송 테스트는 성공했다.
- 라즈베리파이 SSH 접속과 코드 배포, 원격 Python 컴파일은 성공했다.
- 라즈베리파이에서 카메라 목록은 `imx708_wide_noir`로 인식되었다.
- 라즈베리파이에서 mock 파일을 PC로 소켓 전송하는 테스트도 성공했다.
- 재시도 결과 실제 `rpicam-vid` 2초 촬영과 라즈베리파이→PC 소켓 전송도 성공했다.
- 수신 파일은 `1280x720` H.264로 저장되었고, remux 후 OpenCV `VideoCapture`로 첫 프레임 읽기까지 검증했다.
- PC 확인 방식은 HTTP보다 OpenCV `imshow` 창 기반을 기본으로 가져간다.
- OpenCV Qt font 경고 완화를 위해 `run_pc_receiver_opencv.sh`를 추가했고, 포그라운드 실행을 기본 사용 방식으로 정리했다.

### 요청-응답형 JPG 캡처 구조 전환
- PC 소켓 서버가 라즈베리파이에 `capture` 명령을 보내면 라즈베리파이가 JPG 한 장을 촬영해 인코딩된 JPG bytes로 응답하는 구조를 추가했다.
- 추가 파일:
  - `workspaces/지웅/vision/pc_jpeg_capture_server.py`
  - `workspaces/지웅/vision/raspi_jpeg_capture_client.py`
  - `workspaces/지웅/vision/run_pc_jpeg_capture_server.sh`
  - `workspaces/지웅/vision/smoke_jpeg_command.sh`
- 로컬 mock 테스트와 라즈베리파이 실제 카메라 테스트가 모두 성공했다.
- 실제 테스트 결과 PC 서버가 `capture` 명령을 전송했고, 라즈베리파이가 `rpicam-still`로 `1280x720` JPG를 촬영해 PC로 전송했다.
- PC에서 수신한 JPG는 OpenCV로 디코딩되어 shape `(720, 1280, 3)`로 확인되었다.

### Dobot-비전 소켓 연동 계획 수립
- `workspaces/지웅/vision/README.md`와 JPG 캡처 서버/클라이언트 구조를 확인했다.
- `harvest_test.py`에 소켓 통신을 직접 넣지 않고, 별도 `vision_capture_daemon.py`와 얇은 `vision_capture_client.py`로 분리하는 방향을 계획했다.
- 계획 문서는 `docs/Dobot_비전_소켓_연동_계획.md`에 저장했다.
- 다음 구현 후보는 프로토콜 공통 모듈 분리, PC 비전 캡처 데몬 작성, Dobot 쪽 얇은 클라이언트 추가, mock 통합 테스트 순서다.

### Dobot-비전 소켓 연동 구현
- `vision_socket_protocol.py`, `vision_capture_daemon.py`, `vision_capture_client.py`를 추가해 Dobot 모션 코드와 라즈베리파이 소켓 프로토콜을 분리했다.
- `harvest_test.py`에 `--vision-mode wait|socket|off`를 추가했고, socket 모드에서는 각도 `0/120/-120`마다 캡처 요청을 보낸다.
- 테스트 목적상 3장 캡처가 모두 성공하면 `quality_status=normal`로 판정하고, Dobot 노드는 정상 판정에 따라 컨베이어 시작점으로 이동한다.
- 판정 분기 함수도 추가해 추후 YOLO/OpenCV 결과가 `defect`를 반환하면 불량품 상자로 이동할 수 있게 했다.
- 검증 결과: vision pytest 1개, dobot pytest 3개, 기존 JPG smoke, colcon build, ros2 dry-run socket 통합 테스트 모두 통과했다.

### Dobot-비전 소켓 연결 끊김 수정
- 스크린샷 기준 라즈베리파이 클라이언트가 `connected; waiting for capture commands` 이후 `TimeoutError: timed out`로 반복 재접속하고, Dobot 노드는 `socket closed while receiving 16 bytes`로 실패하는 현상을 확인했다.
- 원인은 연결 timeout이 명령 대기 read에도 적용된 점이었다.
- `raspi_jpeg_capture_client.py` 기본 명령 대기 timeout을 무제한으로 바꾸고, 필요 시 명시적으로 `--command-idle-timeout-sec`를 줄 수 있게 했다.
- `vision_capture_daemon.py`는 Pi 소켓이 끊겼을 때 새 Pi 연결을 accept해 1회 재시도하도록 보강했다.
- 회귀 검증: 10초 이상 idle 후 캡처 성공, 기존 smoke, pytest, colcon build, ros2 dry-run socket 3장 캡처 모두 통과했다.

## 2026-06-24

### 오늘 한 일 — 컨베이어 파트 범위 전환
- `README.md`, WBS/R&R, 진행 로그, 작업 결정 메모, `docs/Vision_작업_메모.md`, `docs/시스템_데이터_흐름_초안.md`, `workspaces/지웅/vision/README.md`, `references/WBS.xlsx`를 컨베이어 관점에서 확인했다.
- 이 쓰레드의 지원 범위를 **컨베이어 + D435i + OpenCV + ROI 기반 흐름 확인**으로 정했다.
- 컨베이어 전용 작업 문서 `docs/Conveyor_작업_메모.md`를 추가했다.
- README 문서 목록과 현재 결정 사항에 컨베이어 작업 메모와 MVP 방향을 추가했다.

### 컨베이어 기준 핵심 결과
- 2번 카메라는 D435i를 쓰는 컨베이어 흐름 확인용 보조 비전 카메라로 둔다.
- 컨베이어 파트는 작물 종류/정상·불량을 다시 판정하지 않고, 1번 카메라 판정 이후 정상 작물의 이송 상태만 확인한다.
- 1차 구현은 **RGB 프레임 + 고정 ROI + OpenCV contour/배경 차분 계열 감지**로 시작한다.
- depth는 MVP 필수 기능이 아니라 RGB ROI 감지가 불안정할 때 보조 검증으로 추가한다.
- ROI는 `belt_roi`, `entry_roi`, `middle_roi`, `exit_roi`로 나누고, 이벤트는 `entered`, `moving`, `exited`, `missing`, `stalled`, `misaligned`, `delivered` 중심으로 정리한다.

### 컨베이어 파트 다음 액션
- D435i 실행 위치가 PC 직접 연결인지, 라즈베리파이/별도 보드인지 확정한다.
- 실제 D435i RGB 프레임 1장을 저장하고 ROI 좌표를 그려 보정한다.
- 저장 이미지/동영상 기준으로 OpenCV ROI 감지 스크립트를 먼저 만들고, 이후 실시간 입력과 컨베이어 모터 제어로 확장한다.

### D435i / RealSense ROS 실행 오류 진단
- D435i USB 연결은 `lsusb`에서 `8086:0b3a Intel(R) RealSense(TM) Depth Camera 435i`로 확인되어 정상 인식된다.
- `ros2 launch realsense2_camera rs_launch.py` 오류는 현재 환경에서 `Package 'realsense2_camera' not found`로 재현되었다.
- `ros-humble-realsense2-camera`, `ros-humble-realsense2-camera-msgs`, `librealsense2-utils`, `librealsense2-udev-rules`는 설치되어 있지 않지만 apt 후보는 존재한다.
- 1차 원인은 하드웨어 연결보다 **ROS RealSense wrapper / librealsense 유틸리티 미설치**로 판단했다.
- 권장 조치: `sudo apt install -y ros-humble-realsense2-camera ros-humble-realsense2-camera-msgs librealsense2-utils librealsense2-udev-rules` 후 `rs-enumerate-devices`와 ROS launch를 재검증한다.

### 컨베이어 ROI/Modbus 구현 계획 수립 및 단순화
- 샘플 이미지 `references/realsense-test-image.png`를 확인했고, 컨베이어가 이미지 왼쪽에 있음을 기준으로 계획을 세웠다.
- 사용자 피드백에 따라 감지 대상은 작물이 아니라 **컨베이어 위 빨간색/녹색 큐브**로 정리했다.
- ROI는 `entry/middle/exit`로 나누지 않고, 컨베이어/벨트 영역을 **단일 `conveyor_roi`**로 잡는다.
- 전체 프레임에는 Dobot, 격자판, 버튼 박스, 케이블이 함께 있어 오검출 위험이 있으므로 `conveyor_roi` 내부에서만 빨강/초록 HSV 색상 감지를 수행한다.
- 큐브가 ROI 안에 보이면 컨베이어를 시계방향으로 구동하고, 큐브가 ROI에서 10프레임 연속 미검출되면 이송 완료로 보고 정지하는 상태 머신으로 단순화했다.
- 샘플 기준 초기 좌표 후보는 컨베이어 전체 `x=70~430, y=20~360`, 감지용 ROI `x=140~370, y=35~335`로 잡았다.
- D435i 입력은 ROS 2 `realsense2_camera` topic을 사용하고, 웹에는 추후 원본/ROI annotated frame과 이벤트 상태를 표시하는 방향으로 정했다.
- 컨베이어 모터는 Raspberry Pi에서 동작시키며, 제어는 **Modbus TCP + `pymodbus==3.13.1`**를 사용한다. Modbus는 추후 로봇 동작 상태와 농장 현황을 함께 관리하는 공통 상태/제어 레이어로 확장한다.
- 빨강/초록 큐브는 같은 의미로 처리하는 것으로 확정했다.
- 구현 계획 문서 `docs/Conveyor_ROI_Modbus_구현_계획.md`와 Obsidian [[Conveyor ROI + Modbus 구현 계획]]을 단일 ROI 큐브 감지 방식으로 갱신했다.

### 컨베이어 top-view + ROI 좌표 세팅 도구 구현
- 작업환경은 `/home/ssafy/work/SmartFarmProject/workspaces/지웅/conveyor`로 확정했다.
- 프레임이 비스듬하게 찍히므로 원본 프레임에서 4점을 클릭해 perspective transform으로 top-view를 만든 뒤, top-view 프레임에서 컨베이어 ROI 4점을 다시 클릭해 저장하는 방식으로 정리했다.
- `scripts/select_conveyor_roi.py`를 작성했다.
  - `마우스이벤트1` / `mouse_event_1_topview_quad`: 원본 프레임의 top-view 변환용 4점 클릭
  - `마우스이벤트2` / `mouse_event_2_conveyor_roi`: top-view 프레임의 컨베이어 ROI 4점 클릭
  - 결과 저장: `config/conveyor_roi_topview.json`
  - 확인용 preview 저장: `config/previews/`
- 작업환경 `requirements.txt`에는 `opencv-python`, `numpy`를 기록했다.
- `uv venv .venv`, `uv pip install -r requirements.txt`, `python -m py_compile`, `--help`, `--self-test`까지 검증했다.

### 컨베이어 좌표 세팅 도구 B안 반영
- 실제 detector가 ROS topic frame을 받을 예정이므로 좌표 세팅도 같은 ROS `sensor_msgs/Image` topic에서 직접 받은 프레임으로 수행하도록 수정했다.
- `scripts/select_conveyor_roi.py`에 `--ros-topic` 옵션을 추가했다.
  - 예: `python scripts/select_conveyor_roi.py --ros-topic /camera/camera/color/image_raw --output config/conveyor_roi_topview.json`
  - ROS preview 창에서 `SPACE`를 누르면 현재 ROS frame을 고정하고, 이후 top-view 4점/ROI 4점을 클릭한다.
  - `rgb8`, `bgr8`, `rgba8`, `bgra8`, `mono8` 계열 ROS Image encoding을 OpenCV BGR frame으로 변환하도록 했다.
- JSON에는 `source.source_type=ros_topic`, topic, encoding, frame_id, stamp, 원본 frame width/height를 함께 저장한다.
- 검증: `python -m py_compile`, `--help`, `--self-test`, fake `rgb8` ROS Image 변환 테스트를 통과했다.

### 컨베이어 top-view 출력 크기 문제 수정
- 작업환경 가상환경 Python과 ROS Python 버전이 모두 **3.10.12**로 맞춰진 것을 확인했다.
- 문제 원인: 기존 `warp_topview()`는 top-view 출력 크기를 클릭한 4점 quad의 실제 폭/높이로 계산했다. 그래서 컨베이어 평면을 좁게 찍으면 다음 ROI 선택 창도 작은 프레임으로 떴다.
- 수정: 기본 top-view 출력 크기를 원본 ROS frame과 같은 크기로 유지하는 `--topview-size-mode raw`를 기본값으로 추가했다.
- 기존 방식이 필요하면 `--topview-size-mode quad`를 명시하면 된다.
- JSON에는 `topview.output_size_mode`를 저장하도록 했다.
- 검증: Python 3.10.12 환경에서 `python -m py_compile`, `--help`, `--self-test`, `topview.size_wh == raw_frame.width/height` assertion을 통과했다.

### 컨베이어 top-view 지정 영역 축소 현상 추가 수정
- 스크린샷 `Screenshot from 2026-06-24 11-43-59.png`, `Screenshot from 2026-06-24 11-44-11.png`를 확인했다.
- 사용자가 말한 문제는 단순히 top-view 창 크기가 작은 것이 아니라, 지정한 4점보다 안쪽 영역이 top-view로 잡히는 것처럼 보이는 현상으로 해석했다.
- 기본 point order를 자동 정렬이 아니라 사용자가 클릭한 순서 그대로 사용하는 `--point-order click`으로 명시했다.
- 좌표 입력 창은 `WINDOW_AUTOSIZE`로 띄워 OpenCV 창 리사이즈에 따른 마우스 좌표 오차를 줄였다. 화면이 크면 창을 줄이지 말고 `--display-scale`을 사용한다.
- 경계가 계속 잘려 보일 때를 위해 `--source-padding-ratio` 옵션을 추가했다. 예: `--source-padding-ratio 0.03`.
- JSON에는 `topview.point_order_mode`, `topview.source_padding_ratio`를 저장하도록 했다.
- 검증: Python 3.10.12 환경에서 `py_compile`, `--help`, no-padding/padding self-test, JSON assertion을 통과했다.

### 컨베이어 ROS2 top-view ROI 색상 감지 테스트 노드 추가
- 새 ROS2 패키지 `conveyor_vision_test`를 `/home/ssafy/work/SmartFarmProject/workspaces/지웅/ros2_ws/src/conveyor_vision_test`에 추가했다.
- 실행 노드는 `topview_color_detector`이며, `/camera/camera/color/image_raw`를 구독해 `conveyor_roi_topview.json` 기준 top-view 변환을 수행한다.
- top-view 창에는 `conveyor_roi`를 표시하고, ROI 내부 빨간색/초록색 물체가 발견되면 bounding box와 색상 라벨을 표시한다.
- annotated 결과 이미지는 `/conveyor/topview_annotated`로 publish한다.
- 주요 실행 명령은 `ros2 run conveyor_vision_test topview_color_detector`이며, 창 크기는 `-p display_scale:=0.7`, GUI 비활성화는 `-p show_windows:=false`로 조절한다.
- 검증: `/usr/bin/python3 -m py_compile`, 단위 테스트 `2 passed`, `colcon build --packages-select conveyor_vision_test`, `colcon test --packages-select conveyor_vision_test`, `ros2 run ... -p show_windows:=false` smoke 실행을 통과했다.

### 컨베이어 Modbus TCP client 연동
- Modbus TCP server를 `192.168.110.109:50200`으로 정하고, 기존 동기식 검증 당시에는 `pymodbus==3.9.2`를 `/usr/bin/python3` user site에 설치/확인했다. 이후 async 전환 계획에서 `pymodbus==3.13.1`로 올리기로 했다.
- `conveyor_modbus.py`를 추가해 컨베이어 전용 holding register block `40021~40030` 중 PC/ROS vision-owned `40021/40022/40025~40027` write와 Pi-owned `40023/40024` read 기반 gating을 구현했다. `40028~40030`은 확장 예비로 둔다.
- `conveyor_command`는 `0 stop`, `1 run_clockwise`, `2 run_counter_clockwise`, `3 reset`, `4 emergency_stop`으로 확장했다.
- `topview_color_detector`는 `modbus_enabled:=true`일 때 감지 상태를 Modbus에 쓴다. 기본값은 안전을 위해 `false`다.
- 기본 구동 방향은 `conveyor_run_command:=run_clockwise`이며, 반시계 방향은 `conveyor_run_command:=run_counter_clockwise`로 실행한다.
- ROI 안에서 큐브가 감지되면 running 상태/색상/이벤트를 쓰고, 10프레임 연속 미검출되면 `stop + delivered`를 쓴다.
- 수동 명령 엔트리포인트 `conveyor_modbus_command`를 추가해 `emergency_stop`, `reset`, `run_clockwise`, `run_counter_clockwise`, `stop`을 바로 보낼 수 있게 했다.
- 검증: `py_compile`, 단위 테스트 `7 passed`, `colcon build`, `colcon test`(`7 tests, 0 errors, 0 failures`), `conveyor_modbus_command emergency_stop --dry-run`, `topview_color_detector modbus_dry_run` smoke 실행을 통과했다.

### 컨베이어 Modbus async / Raspberry Pi 5 전환 계획 수립
- 사용자 피드백으로 기존 `pymodbus==3.9.2` 동기식 client 구조에서 Modbus read/write 시 버벅임이 발생할 수 있음을 반영했다.
- Modbus 기준 버전을 **`pymodbus==3.13.1`**로 변경하고, `AsyncModbusTcpClient` 기반 구조로 전환하는 계획을 세웠다.
- 이 시점의 초기 계획에는 Raspberry Pi 5/Raspbian이 Modbus TCP server와 컨베이어 GPIO 모터 제어를 함께 담당하는 안이 포함되어 있었으나, 이후 사용자가 외부 Modbus server `192.168.110.109:50200`을 확정하면서 Pi는 **Modbus client + GPIO controller**로 정정했다.
- 사용자 제공 참고 코드 `workspaces/지웅/conveyor/ref/realsense/*`, `workspaces/지웅/conveyor/ref/conveyor/conveyor_test.py`를 계획에 반영했다.
- 전환 계획 문서 `docs/Conveyor_Modbus_Async_RaspberryPi_전환_계획.md`와 Obsidian `[[Conveyor Modbus Async + Raspberry Pi 전환 계획]]`을 추가했다.

### 컨베이어 Modbus server / Raspberry Pi client 역할 정정
- 사용자가 Modbus server를 `192.168.110.109:50200`으로 확정했다.
- Raspberry Pi 5/Raspbian 접속 대상은 `ssafy@192.168.110.139`이며, 보안을 위해 비밀번호는 문서에 저장하지 않는다.
- 이에 따라 Raspberry Pi는 Modbus server가 아니라 **Modbus client**로 외부 server에 접속해 command/speed register를 읽고, 실제 모터 상태를 status/error register에 쓰는 구조로 정정했다.
- PC/ROS2 D435i 비전 노드도 같은 server에 붙는 Modbus client이며, ROS image callback blocking 방지를 위해 async worker 구조를 유지한다.
- 전환 계획 문서와 Obsidian 계획 문서를 이 역할 분리에 맞게 갱신했다.

### Raspberry Pi Modbus client + GPIO controller 계획 수립
- 라즈베리파이에 올릴 컨베이어 제어 클라이언트 계획을 세웠다.
- 계획은 `pymodbus==3.13.1` async Modbus client, `gpiod` 기반 `DIR/STEP/ENABLE` 제어, 버튼 긴급정지/재시작 처리를 한 프로그램에 포함하는 구조다.
- 속도/가감속은 `workspaces/지웅/conveyor/ref/conveyor/conv_profile_지웅.py`의 `TargetSpeed=0.0001`, `InitialSpeed=0.0005`, `RATIO=0.0000005`를 기준으로 반영한다.
- 기존 버튼은 `GPIO23=긴급정지`, `GPIO24=재시작`으로 계획했다.
- 상세 계획 문서 `docs/Conveyor_Pi_Modbus_Client_Controller_구현_계획.md`와 Obsidian `[[Conveyor Pi Modbus Client Controller 구현 계획]]`을 추가했다.

### Raspberry Pi controller 구현/배포
- Pi용 Modbus client + GPIO controller 코드를 구현했다.
- 구현 파일은 `workspaces/지웅/conveyor/pi_controller/` 아래에 두었다.
- 긴급정지/재시작 버튼은 command register를 바꾸지 않고 `40023/40024` status/error만 갱신하도록 반영했다.
- 로컬 검증: `py_compile` 통과, `pytest 13 passed`, dry-run smoke 성공.
- Raspberry Pi `ssafy@192.168.110.139`에 배포했고, `/home/ssafy/SmartFarmProject/workspaces/지웅/conveyor/pi_controller`에 `.venv`를 구성했다.
- Pi에서 `pymodbus==3.13.1` 설치와 `gpiod import ok`를 확인했다.
- `run_pi_controller.sh` 실행 권한을 설정했다.
- 현재 Pi에서 Modbus server `192.168.110.109:50200`은 `No route to host`로 접속되지 않아, 실제 server 연동은 네트워크 확인 후 재검증이 필요하다.

### 컨베이어 Modbus server 파트 집중 범위 정리
- README/docs/WBS/R&R/진행 로그와 컨베이어 Modbus 관련 계획 문서를 다시 확인했다.
- 이 쓰레드의 집중 범위를 **Modbus TCP server / shared register layer**로 좁혔다. PC/ROS2 vision node와 Raspberry Pi GPIO controller는 server client/interface 수준에서만 다룬다.
- Modbus server endpoint는 `192.168.110.109:50200`, Raspberry Pi는 `ssafy@192.168.110.139`의 **Modbus client + GPIO controller**로 정리했다.
- server 전용 작업 메모 `docs/Conveyor_Modbus_Server_작업_메모.md`와 Obsidian `[[Conveyor Modbus Server 작업 메모]]`를 추가했다.
- 기존 결정 메모의 “Pi가 server”로 읽히는 문구를 외부 server + Pi client 구조로 정정했다.
- 현재 이 PC에서 TCP 확인 결과 `192.168.110.109:50200`, `192.168.110.139:22` 모두 timeout이므로, 실제 server 연동 전 네트워크/listen 상태 확인이 필요하다.

### RealSense ROS 비전 노드 Modbus command gating 구현
- 사용자가 Pi 쪽 실제 장비 테스트는 직접 하기로 했고, ROS D435i/top-view ROI 노드에 객체 감지 시 Modbus command를 내리는 로직을 보강했다.
- 기존 전체 register block write 방식에서, PC/ROS 비전 노드는 `40021/40022/40025~40027`만 쓰고 Pi가 `40023/40024`를 소유하는 방식으로 수정했다.
- `topview_color_detector`는 command write 직전에 `40023 conveyor_status`, `40024 conveyor_error_code`를 읽고, Pi가 `emergency_stopped`이면 `40021/40022`를 쓰지 않는다.
- 상태 정책은 `idle/running/delivered`에서 run/stop 허용, `error`에서는 run/reset 금지와 stop만 허용, status read 실패 시 fail-safe로 command 없이 vision register만 write로 정했다.
- 검증: `/usr/bin/python3 -m pytest test/test_topview_color_detector.py -q` 결과 `11 passed`, `py_compile` 통과, `colcon build --packages-select conveyor_vision_test` 통과, `colcon test` 결과 `11 tests, 0 errors, 0 failures, 0 skipped`.

### Modbus server 중앙 작업공간 정리 및 endpoint 변경
- 사용자가 Modbus server 관리 위치를 `/home/ssafy/work/SmartFarmProject/workspaces/지웅/modbus`로 확정했다.
- server endpoint를 `192.168.110.109:50200`으로 변경했다.
- 기존 컨베이어/RealSense/Pi controller 쪽에 흩어진 pymodbus 예제와 register map 내용을 새 Modbus 작업공간으로 가져와 정리했다.
- 새 파일: `workspaces/지웅/modbus/README.md`, `register_map.py`, `modbus_server.py`, `modbus_client_smoke.py`, `tests/test_register_map.py`, `references/*`.
- Modbus server를 컨베이어 전용이 아니라 Dobot/TurtleBot/농장 상태까지 담는 shared register layer로 확장하기 위해 register block을 예약했다: 컨베이어 `40021~40030`, Dobot `40031~40050`, TurtleBot `40051~40070`, system/farm `40071~40100`.
- 프로젝트 docs와 Obsidian에 `Modbus Server 작업 메모`를 추가하고 기존 `192.168.110.107` 참조를 `192.168.110.109` 기준으로 갱신했다.

### Modbus server 로컬 실행 검증
- 현재 PC가 `192.168.110.109/24` 주소를 가진 것을 확인했다.
- `/home/ssafy/work/SmartFarmProject/workspaces/지웅/modbus`에서 `.venv`를 만들고 `pymodbus==3.13.1`을 설치했다.
- `register_map.py`, `modbus_server.py`, `modbus_client_smoke.py` py_compile과 register_map 테스트를 통과했다.
- `modbus_server.py --host 192.168.110.109 --port 50200`로 서버를 실행했고, `modbus_client_smoke.py --write-demo`에서 read/write smoke가 성공했다.
- 기존 ROS2 conveyor manual client도 새 endpoint `192.168.110.109:50200`으로 `stop` command write가 성공했다.
- 현재 server background session은 `proc_97a93c7ffdc8`이다.

## 2026-06-24 프로젝트 구조 정리

### GitHub / 로컬 상태 확인
- GitHub 저장소 `akdlxm39/SmartFarmProject`의 `main` 브랜치와 로컬이 `0 ahead / 0 behind`로 동기화되어 있음을 확인했다.
- Open PR/Issue는 없는 상태로 확인했다.

### docs 재분류
- 기존 평면형 `docs/` 구조를 다음 기준으로 재분류했다.
  - `docs/00_project/`: 프로젝트 개요, R&R, 착수 체크리스트
  - `docs/10_architecture/`: 시스템 흐름, 아키텍처 계획, 시뮬레이션/현실 차이
  - `docs/20_subsystems/`: vision, dobot, conveyor, modbus, turtlebot, web
  - `docs/30_plans/`: WBS 검토, 구현 계획, 폴더 구조 정리 계획
  - `docs/40_logs/`: 진행 로그, 결정 메모
  - `docs/diagrams/`: Excalidraw 등 다이어그램 산출물

### 공통 skeleton 생성
- `apps/`, `robot/`, `vision/`, `embedded/`, `modbus/`, `config/`, `data/` 공통 폴더 skeleton과 각 README를 생성했다.
- `workspaces/`는 바로 제거하지 않고 개인별 초기 작업 보관소로 유지한다.
- 통합 가능한 코드는 이후 `modbus/shared_server`부터 순차적으로 승격할 계획이다.

### README 갱신
- 루트 `README.md`를 새 통합 폴더 구조 기준으로 갱신했다.
- 다음 승격 순서는 Modbus server -> Conveyor Pi -> D435i conveyor vision -> Dobot ROS2 -> Camera1/YOLO -> frontend/backend 순서로 정리했다.
